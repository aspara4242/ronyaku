name: Redeploy Vercel Site

on:
  schedule:
    - cron: "0 17 * * *"  # Runs every day at 17:00 UTC (2:00 JST)
  workflow_dispatch:

jobs:
  redeploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
